# In this directory, prefer the repo-local CLI over any global install or shell alias.
# This makes `sma -v` show the version from `../../package/bin/cli.js`.
unalias sma 2>/dev/null || true

export PATH="$PWD/bin:$PWD/node_modules/.bin:$PATH"
